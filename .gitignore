# deps / build
node_modules/
.next/
*.tsbuildinfo

# secrets and machine-local deploy state (never commit)
.env*
.vercel/

# local tooling artifacts
.playwright-mcp/
.DS_Store

# Apple Maps reference captures: used privately for the beauty loop,
# not redistributable. design/ref/NOTES.md stays.
design/ref/*.png

# generated beauty-loop plate galleries (heavy, reproducible via
# scripts/render-plates.ts)
design/loops/

# scratch screenshots at repo root
/*.png
